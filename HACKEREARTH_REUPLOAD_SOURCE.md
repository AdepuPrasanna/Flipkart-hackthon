# Re-upload source on HackerEarth (notebook + presentation)

Open: [Traffic demand prediction](https://www.hackerearth.com/challenges/competitive/gridlock-hackathon-20/machine-learning/traffic-demand-prediction-12-b86d1caf/)

## Files to upload (do all three steps)

### Step A — Zip (must list 7 files inside)

Upload **`gridlock_source_submission.zip`** from:

- `C:\Users\munna\Downloads\gridlock_source_submission.zip` (fresh copy), or  
- repo root after `python scripts/build_source_zip.py`

Open the zip and confirm you see:

1. `ZIP_MANIFEST.txt`
2. `approach.txt`
3. `predict.py`
4. `requirements.txt`
5. `README.txt`
6. **`traffic_demand_solution.ipynb`**
7. **`Gridlock_Presentation.pptx`**

If you only see 4 files, you have an **old zip** — delete it and rebuild.

### Step B — Notebook (separate upload on HackerEarth)

Upload **`traffic_demand_solution.ipynb`** from folder:

`source_submission\traffic_demand_solution.ipynb`

### Step C — Presentation (separate upload)

Upload **`Gridlock_Presentation.pptx`** from folder:

`source_submission\Gridlock_Presentation.pptx`

HackerEarth instructions ask for **.ipynb + presentation in addition to** the zip. Upload all three even if the zip already contains them.

## Your Answer (optional text)

```
See approach.txt in the source zip. Jupyter notebook: traffic_demand_solution.ipynb.
Slides: Gridlock_Presentation.pptx. CLI: predict.py. Leaderboard submission ID 128898705 (score 100).
```

Then click **Submit** / **Upload** as required.
